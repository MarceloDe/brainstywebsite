/** Shared concierge A2UI constants — framework-agnostic (server + client + assembler). */
export const CONCIERGE_CATALOG_ID = 'brainsty-concierge-v1';
export const CONCIERGE_SURFACE_ID = 'concierge-canvas';
/** Action name every interactive block dispatches; context carries { value: intent }. */
export const SELECT_ACTION = 'select';
